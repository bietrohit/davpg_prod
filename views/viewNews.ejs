<%- include('./partials/admin_header') -%>
<body>
<h2 style="text-align: center; margin-top: 20px;">News/Events/Marquee</h2>
<div class="row" style="margin-left: 100px; margin-right: 100px;margin-top: 20px;">
    <%- include('./partials/admin_menu') -%>
    <div class="col-10" style="text-align: center;">
      <table class="table table-striped table-primary">
          <thead>
            <tr>
              <th>Title</th>
              <th>Type</th>
              <th>Delete</th>
            </tr>
          </thead>
          <tbody>
            <%if (send.length >0){ %>
            <% send.forEach(function(row){ %>
            <tr>
              <td><%= row.Title %></td>
              <td><%= row.Type %></td>
              <td><a href="/delete_news/<%= row.Id %>/<%= row.Link %>">Delete</a></td>
            </tr>
            <% })}else{ %>
              <tr>
                  <td colspan="6">No Record Found</td>
                  
                </tr>
              <% } %>
          </tbody>
        </table>
    </div>
</div>
</body>
</html>